import React from 'react'

const FooterCircle = ({ className }) => {
    return (
        <div className={`bg-blue w-2 h-2 rounded-full ${className}`}>
        </div>
    )
}

export default FooterCircle